# Dependencies
node_modules/

# Environment / secrets
.env
.env.local
.env.*.local

# Next.js
.next/
out/

# Build output
dist/
build/

# Prisma
# (keep schema.prisma tracked, but not generated client artifacts if any live outside node_modules)

# Docker
docker-compose.override.yml

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/settings.json
.idea/

# Local model files (if any get downloaded outside Docker/Ollama's own storage)
*.gguf
*.bin

# Baseline raw output (keep the summarized phase0-baseline.md, not the raw dump, if it's large)
docs/phase0-baseline-raw.txt
/app/generated/prisma
