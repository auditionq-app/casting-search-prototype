# R&D kits

| Kit | Location | Purpose |
|-----|----------|---------|
| [`director-applicant-search/`](director-applicant-search/) | `director-applicant-search-rd-kit/director-applicant-search/` | Director search / filter / rank of talents who applied to a casting call |

See the kit's `README.md` (inside the folder above) for install and usage. This is the actual, currently-used location in this project — ignore any references elsewhere to a `.zip` or an `rd-kits/` path; those describe how this kit was originally packaged/shared, not where it lives in this workspace.